/*
void setup(){
  size(500,500);
  println("Sono nel setup");
  frameRate(30); // numero di volte al secondo in cui viene avviata draw
  background(0);
}

void draw(){
  //if(frameCount == 150) // per evitare situaizoi di frameskip dovremmo fare if(frameCount>150 && frame<200)
  if(millis()>3000 && millis()<4000) // millis ritorna il numero di millisecondi da quando ho avviato lo script
  ellipse(width/2, height/2,50, 50);
  println("Sono nel draw"+frameCount); //frameCount è la variabile che contiene il numero di frame;
}
*/
